{
  "name": "dominance",
  "version": "1.0.0",
  "scripts": {
    "setup": "cd server && npm install --omit=dev && cd ../client && npm install --omit=dev",
    "seed": "cd server && node data/seed.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "start": "concurrently \"cd server && npm start\" \"cd client && npm run preview\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
